import { NotificationDisplay } from "@/components/notification-display"

export default function NotificationPage() {
    return (
      <main>
        <h1>Notifications</h1>
        <NotificationDisplay appType="default" />
      </main>
    );
  }